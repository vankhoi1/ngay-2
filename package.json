{
  "name": "img-proxy",
  "version": "1.0.0",
  "description": "Local image proxy for Imgur (development use)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
